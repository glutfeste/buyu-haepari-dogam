filters:
  and:
    - file.hasTag("이야기")
    - file.folder.contains("에피소드")
    - note.순서 >= 0
properties:
  note.순서:
    displayName: 순서
views:
  - type: table
    name: 에피소드
    order:
      - 순서
      - file.name
    sort:
      - property: 순서
        direction: ASC
