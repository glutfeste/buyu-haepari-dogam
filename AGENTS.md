---
tags: [창작, 메타]
---

# 부유 해파리 도감 — 에이전트 지침

이 파일은 이 저장소를 다루는 모든 작업 에이전트가 따르는 하나의 기준 문서다. 에이전트는 저장소의 파일을 읽고, 분석하고, 수정하는 자동화 작업 도구이며, Codex·Claude 같은 도구가 여기에 해당한다.

프로젝트의 설정과 작품 내용은 각 설정·이야기 문서가 현재의 기준이다. 이 파일은 작업 방법과 문서 작성 규칙을 정한다. 별도의 작업지침 문서나 업데이트 로그는 두지 않는다. 변경 이력은 Git의 커밋·diff·revert 기록으로 관리한다.

## 사용자용 — GitHub 최신본을 옵시디언 볼트에 덮어쓰기

GitHub의 `main` 브랜치를 프로젝트 원본으로 본다. `C:\Users\user\Documents\2026-04부터\전체\창작\부유해파리 도감` 로컬 폴더는 작업·열람용 복사본이며, 사용자가 최신본을 내려받을 때 로컬의 추적 파일 변경은 보존하지 않고 GitHub 상태로 덮어쓰는 것이 의도다.

PowerShell을 열고 아래 한 줄을 그대로 붙여넣고 Enter를 누른다.

```powershell
Set-Location 'C:\Users\user\Documents\2026-04부터\전체\창작\부유해파리 도감'; if (-not (Test-Path .git)) { powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\restore-git-metadata.ps1 }; git fetch origin main; git reset --hard origin/main
```

이 명령은 GitHub `main`의 추적 파일과 동일하게 로컬 파일을 덮어쓰며, 커밋하지 않은 로컬 수정도 버린다. `git clean`은 실행하지 않으므로 Git이 추적하지 않는 로컬 전용 파일은 삭제하지 않는다. 에이전트가 GitHub를 수정한 뒤 사용자가 로컬 옵시디언 볼트에 최신본을 받고 싶다고 하면 위 한 줄만 안내한다.

## 프로젝트 개요

떠 있는 세계를 무대로 한 창작 설정 보관소다. 넬라 베린디스가 반려 거품해파리 보글이와 비행선을 갈아타며 세계를 돌고, 부유 해파리를 연구하여 분류하고 도감에 기재한다. 그 여정에 보레아 영구기관과 카를 얀센을 둘러싼 줄거리가 흐른다.

## 적용 범위와 우선순위

- 사용자가 질문과 편집을 함께 요청하면 질문에 먼저 말로 답하고 그다음 편집한다. 질문이 여럿이면 하나씩 모두 답한다.
- 사용자가 읽거나 수정할 파일·폴더를 지정하면 그 범위를 따른다. 사용자가 읽지 말라고 한 파일은 작업에 필요해 보여도 임의로 열지 않는다.
- 설정을 수정할 때는 수정 대상의 현재 문서와 직접 이어지는 문서를 확인한다. 세계 전체의 맥락이 필요할 때만 설정 바이블을 추가로 읽는다.
- Git 기록은 변경의 시점과 이유를 확인하는 자료다. 커밋 메시지와 과거 diff를 현재 설정으로 옮기지 않는다. 현재 설정은 현재 문서에 적힌 내용으로 판단한다.
- 이해했음을 보이려고 다른 문서에 이미 있는 원리나 설명을 되풀이하는 문장을 넣지 않는다. 독립된 주제는 대표 문서에 적고 다른 문서에서는 링크로 가리킨다.

## 작업 절차와 Git

- 모든 읽기·편집·Git 명령은 `C:\Users\user\Documents\2026-04부터\전체\창작\부유해파리 도감`에서 수행한다.
- `P:\2026-04부터\전체\창작\부유해파리 도감`은 pCloud 양방향 동기화 복제본으로만 사용한다. 작업 경로와 Git 작업 트리는 C: 저장소로 고정한다.
- pCloud 동기화에서 `.git`을 제외한다. Git 메타데이터는 C: 작업 폴더와 GitHub에 둔다.
- **`main`은 이 저장소의 유일한 작업 정본 브랜치다.** 사용자가 브랜치 생성을 명시적으로 지시한 경우에만 다른 브랜치를 만들 수 있다.
- 리뷰·백업·실험·발행·안전장치라는 이유로 에이전트가 임의로 브랜치를 만들지 않는다. 사용자가 매 작업에서 브랜치 생성을 금지하는 말을 반복하지 않았다는 사실을 허가로 해석하지 않는다.
- 비-`main` 브랜치를 과거 작업 보관소나 평행 정본으로 유지하지 않는다. 완료된 작업과 과거 상태는 Git 커밋·diff·revert 기록으로 확인한다.

### 실행 체크리스트

- 실행 체크리스트는 현재 작업 범위와 미완료 항목을 추적하는 임시 작업 파일이다. 작업 중에는 완료 항목을 `[x]`로 갱신한다.
- 마지막 전역 감사까지 모든 항목을 완료하면 체크리스트 파일 자체를 즉시 삭제한다. 완료본·보관본·백업본·과거 체크리스트 사본을 저장소에 남기지 않는다.
- 완료한 작업의 과거 이력은 Git 커밋과 diff에만 남긴다. 새 작업에서 과거 완료 체크리스트를 복원하거나 누적 작업일지로 키우지 않는다.
- 새 작업에 체크리스트가 필요하면 현재 정본과 새 작업 범위를 기준으로 새 파일을 만든다. 활성 체크리스트가 있으면 작업 전에 처음부터 끝까지 읽고, 사용자가 보존 확정한 항목과 승인한 개고 범위를 그대로 따른다.

1. 작업을 시작하기 전에 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\git-preflight.ps1`을 실행해 저장소 루트·origin·main 브랜치·HEAD·커밋 작성자 설정을 확인하고, 현재 상태와 요청 범위 밖의 변경을 살핀다. 점검이 실패하면 문서 편집을 시작하지 말고 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\restore-git-metadata.ps1`로 원격의 Git 메타데이터를 복구한 뒤 다시 점검한다.
2. 편집 대상과 직접 이어지는 현재 문서를 읽는다. 최근 변경의 맥락이 필요하면 해당 경로의 Git 기록과 diff를 확인한다.
3. 설정을 고치면 그 사실이 놓일 대표 문서를 먼저 고치고, 같은 작업 단위 안에서 연관 문서를 정렬한다.
4. 그림을 바꾸면 파일을 다시 쓰고, 삽입된 문서에서 위치와 표시를 확인한다.
5. 문서 전체에서 링크·속성·문장 규칙과 설정 훼손을 확인한다. Git diff를 읽고 diff --check로 공백 오류를 검사한다.
6. 작업 단위가 끝나면 무엇을 왜 고쳤는지 드러나는 제목으로 커밋한다. 본문에는 고친 항목을 나열한다.
7. 커밋한 뒤 곧바로 origin의 main 브랜치에 push한다.
8. 커밋 전에 잘못 고친 내용은 git restore로 되돌린다. 커밋한 뒤 잘못 고친 내용은 git revert로 되돌린다.

저장소는 https://github.com/glutfeste/buyu-haepari-dogam 이고 비공개다. 로컬 폴더와 이 저장소가 origin으로 연결되어 있다. Git이 변경 이력의 유일한 기록이므로 별도 변경 로그를 만들거나 문서 본문에 수정 경위를 적지 않는다.

`.git`은 작업 폴더에 있어야 한다. 메타데이터가 사라졌을 때 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\restore-git-metadata.ps1`은 임시 clone에서 `.git`만 복사하고 `git reset --mixed HEAD`로 index를 재구성하며, 원격 최신 커밋의 작성자 정보를 이 저장소에 설정한다. 작업 파일은 덮어쓰지 않으며, 복구 뒤 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\git-preflight.ps1`을 다시 실행한다.

## 작성 태도

- 받아적지 않는다. 논의에서 나온 사실을 그대로 옮겨 나열하지 말고, 직접 창작하여 구체적 장면과 세부로 살을 붙여 쓴다.
- '새로 써라'는 지시는 문장 단위 수정이 아니라 문서 전면 재작성이다. 기존 뼈대를 남기지 않는다.
- 문서를 손볼 때는 단일 출처(한 내용은 한 문서에만), 부정문·대조문 금지, 넓은 고유어 대신 적확한 낱말 — 이 세 기준으로 문서 전체를 점검한 뒤 마친다. 한 곳씩 걸리게 두지 않는다.
- 근거 없는 단정(예: '절멸의 병기')을 넣지 않는다.
- 사용자가 던지는 질문에는 편집보다 먼저 말로 답한다. 질문이 여럿이면 하나씩 모두 답한다.
- 이해했음을 보이려고 문장을 끼워 넣지 않는다. 방금 배운 원리를 그 자리의 문서에 옮겨 적는 것이 이 실수의 대표 형태다. 원리는 대표 문서에 있고 개별 문서는 링크로 가리킨다.

## 문서 문장 규칙

설정 문서는 자연스러운 문장으로 쓴다. 어미를 '~한다' 한 가지로 묶지 않고, 늘 일어나는 일과 할 수 있는 일을 뜻에 맞는 어미로 구분한다. 능력·가능성은 '~할 수 있다'로 적는다.

이 규칙들의 목적은 평이함이 아니라 적확함이다. 쉬운 말로 풀어 쓰는 것을 목표로 삼지 않는다. 법률·군사·학술 영역을 서술할 때는 그 영역의 전문 용어를 쓴다.

### 쓰지 않는 표현

- 'A가 아니라 B다' 꼴의 대조. 하고 싶은 말을 곧바로 긍정으로 적는다.
- 강조하려고 덧붙이는 '없다·아니다·않다·못한다'. 부정이 진짜 핵심일 때만 쓴다.
- '여기에는 두 가지가 필요하다' 같은 수를 못 박는 한정·열거. 항목이 실제로 닫혀 있더라도 수를 선언하지 않고 각 항목을 직접 서술한다. 항목마다 서술할 내용이 많으면 문서를 새로 만들어 분리한다.
- 버려진 안이나 과거 원고를 부정하기 위해서만 존재하는 문장. 지금의 설정만 서술한다.
- '이름은 임시이며', '자세한 설정은 앞으로 정한다' 같은 미정·임시 상태를 알리는 문구. 정해진 내용만 적고, 정하지 않은 것은 비워 둔다.
- 한 사실을 다른 사례나 일반 규칙에 견주는 비교·유추. '다른 구문사가 제 장치를 지니듯' 같은 구문을 붙이지 말고 그 사실만 직접 적는다.
- 은유·직유·의인화. 생김새 정보를 주는 외형 비교(매를 닮았다, 가오리처럼 넓은 피막)만 허용한다.
- '~인 셈이다', '~라 할 수 있다' 같은 얼버무림.
- 서술문의 반사 소유격 '제'(제 도감·제 배·제 이론)와 '저'(저에게·저만의). '자신의·자기의·그의'로 적고, '제가 ~' 꼴도 '자신이·그가'로 적는다. 인물의 대사는 예외로 둔다.
- 다음 낱말의 환유 용법. 본래의 사전적 대상을 가리킬 때만 쓴다: 결, 박자, 호흡, 톤, 빛깔, 색채, 색깔, 무게, 온도, 농도, 밀도, 질감, 기운, 향기, 분위기, 손, 발, 입, 눈, 귀, 머리, 가슴, 자락, 가닥, 갈래, 줄기, 측면, 지점, 자리, 영역, 차원, 층위, 선상, 궤도, 지평, 흐름, 양상, 양태, 행보, 궤적, 방향성. 환유를 쓸 곳은 구체적인 동사·형용사·명사로 푼다.
- '공명'. 신호와 진동의 상호작용은 수신·감지·재현·전달처럼 그 자리의 일을 가리키는 낱말로 적는다.
- 마법을 가리키는 '설계'·'주문'·'판본'. 두뇌에서 실행되는 마법의 글은 '구문'이다. 기계·제도·건축물을 본래 의미로 설계한다고 적는 것은 그대로 둔다.
- 다른 창작물의 제목.

### 낱말 고르기

- 어느 문장에 넣어도 뜻이 통하는 넓은 동사(들다, 서다, 남다, 열다, 닫다, 취하다, 키우다, 다듬다, 닿다, 꺾다, 메우다, 내주다, 지키다, 치다, 맡다 등)를 본래 의미가 아닌 자리에 쓰지 않는다. 그 자리의 행위를 정확히 가리키는 동사(포함된다, 필요하다, 공급한다, 조절한다, 유지한다, 가격한다 등)로 적는다. '닿는다·도달한다·명중한다·접촉한다'는 서로 다른 일을 가리키므로, 그 자리에서 일어나는 일에 맞는 낱말 하나를 골라 쓴다.
- 같은 뜻을 가리키는 낱말이 여럿이면 의미 폭이 가장 좁은 낱말을 쓴다. 고유어와 한자어 사이에 우열은 없으며, 한자어가 더 협소하면 한자어를 쓴다. '공여한다'를 '내준다'로, '개설한다'를 '연다'로, '방지하기 위하여'를 '막으려'로 바꾸는 것은 이 규칙의 위반이다.
- 여러 속성으로 해석되는 형용사를 피하고 어느 속성인지 지정한다. '구문이 크다' 대신 '구문이 복잡하다'처럼 가리키는 속성을 정확히 지정하는 낱말을 쓴다.
- 능력·규모·성능은 막연한 말(먼 거리, 가까이, 큰 위력, 두꺼운 벽)로 적지 않고 구체적 수치(미터·센티미터·초·그램 등)로 적는다. 요구된 구체값을 '앞으로 정한다'로 미루지 않는다.
- 대명사(이들·그것·여기)가 가리키는 대상이 헷갈릴 수 있으면 대명사 대신 그 대상을 명시한다.
- 한 낱말이 서로 다른 두 가지를 함께 가리키게 두지 않는다. 두 가지가 각각 무엇인지 정하고 낱말을 나눈다.

### 예외

작중 문헌의 조문과 인용문은 그 문서가 실제로 쓰였을 격식 문체(법률문어·칙령체·논설체)를 따른다. 시의 원문과 독음, 등장인물의 직접 대사도 예외다. 이 문장 규칙은 그 바깥의 설정 서술에만 적용한다.

## 문서 배치 규칙

- 한 주제의 본문은 그 주제의 대표 문서 한 곳에만 적는다. 다른 문서에서는 한두 문장으로 요약하고 링크로 가리킨다.
- 구문 문서에는 구문만, 기물 문서에는 기물만 적는다. 구문과 기물을 결합해 쓰는 서술, 누가 무엇으로 무엇과 함께 쓰는지는 그 사용자(인물) 문서에 적고, 구문·기물 문서에서는 링크로만 가리킨다.
- 세계 문서(`세계.md`)는 행성의 대기·물·지질 서술만 담는다. 세계 문서는 작을수록 좋다. 부상석·생태·에너지·세력처럼 제 문서가 있는 주제는 그 문서에 적는다.
- 세상에 알려진 사실과 어긋나는 진실(인물의 생존·정체, 사건의 숨은 원인)은 그 사실이 놓일 절에 그대로 적고, 전말이 온전히 적힌 문서로 링크한다. 숨은 진실만 따로 모으는 절을 두지 않는다.
- 세계의 사실은 `설정\`에 적고, 한 작품의 전개와 구상은 `이야기\`에 적는다. 작품에서 비롯한 사실이라도 세계에 속하는 것(인물의 정체, 기구, 사건의 결말)은 `설정\`의 해당 문서에 적고 `이야기\` 문서는 링크로 가리킨다.
- 소제목은 그 절이 알려 주는 것을 적는다. 소재를 명사 하나로 뽑아 다는 제목(모순·구조·배열)은 목차만 보아서는 무엇을 알게 되는지 드러내지 못한다.

## 폴더 구조

- `부유해파리 도감\` — 프로젝트 뿌리. AGENTS.md가 여기 있다. 변경 이력은 Git 기록에 있다. 프로젝트와 작품의 제목은 『부유 해파리 도감』이다. 문서 표제에는 주제 이름만 쓴다.
- `설정\` — 설정 문서와 그림. 길잡이인 설정 바이블(`부유해파리도감 설정 바이블.md`)이 뿌리에 있고, 문서는 주제별 폴더에 둔다.
- `설정\세계\` — 행성·물질 문서(`세계.md`·`천문.md`·`부상석.md`·`양소.md`·`축열석.md`)와 단면 그림(`부유세계_단면.svg`).
- `설정\생활\` — 문명의 살림 문서(`에너지.md`·`화폐.md`·`비행선.md`·`달력과 시간.md`·`식문화.md`·`양소선.md`·`유체적분기.md`).
- `설정\생태\` — 생물 문서. 대표 문서 `생태.md`가 먹이 사슬과 사는 높이로 생물을 묶고, 개별 생물 문서(공중 플랑크톤·창공 해조류 군락·군락 초식동물·고래·용 등)를 그 아래에 둔다. 종이 여럿인 무리는 하위 폴더로 나눈다. `생태\해파리\`(대표 `부유 해파리.md`와 개별 종), `생태\활공수\`(대표 `활공수.md`와 대양·대륙 숲 갈래), `생태\아랫하늘\`(밑동 따개비·거꾸로 박쥐·아랫하늘 낚시꾼·나락살이)이다.
- `설정\구문\` — 구문 문서. 대표 문서 `구문.md`와 `차분기관.md`가 이 폴더에 있고, 분류별 폴더(증폭·방출·변성·관측·사역·부여)에 개별 구문 문서를 둔다. 구문 구조 그림(`부유세계_구문구조.svg`)도 여기 있다. 도약의 구문은 어느 분류에 넣을지 오래 논쟁되었으나 변성으로 분류해 `변성\`에 둔다.
- `설정\세력\` — 세력별 문서. `강대국\`에 네 강대국(벡실리아 공화국·글루트페스테·오르다·뤼메리-훈디아 이중제국), 종주국별 `~ 권속\` 폴더에 그에 딸린 구성국·자치령·속령·보호국·위성국·괴뢰국·특허회사, `독립 세력\`에 어느 강대국에도 딸리지 않은 세력(뤼흐트스타트·보레아 영구기관·시발룬 제국·메디아나 자유시·유랑방주·대현·아사란), `옛 세력\`에 멸망하거나 계승된 세력(아우렐리아 제국·오피름 제국·뢰벤하임 왕국 등)을 둔다. 종주국이 어느 강대국에도 딸리지 않으면 권속을 거느려도 `독립 세력\`에 두고, 그 권속은 `~ 권속\` 폴더에 둔다(뤼흐트스타트·대현).
- `설정\지리\` — 지리 문서. 위계대로 폴더를 나눈다. `지리\` 바로 아래에 부초별 폴더(아우렐리아·메루아·현초·잔다라·보레아·아스틀로아·아비도라·골롬트)를 두고, 아우렐리아 아래에 권역별 폴더(아이스투아리아·베스페라·글루트·스톨·오렌투스)를 둔다. 새 지명 문서는 그 지명이 속한 상위 지역 폴더에 넣는다. 폴더 이름과 문서 이름에는 '권역'이나 '바깥' 같은 접미사·묶음말을 붙이지 않고, 위계는 속성으로 적는다. 아우렐리아 평면 지도(`부유세계_지도.svg`)와 측면(`부유세계_아우렐리아_측면.svg`)은 `지리\아우렐리아\`에 있다.
- `설정\조직\` — 군사·행정 조직 문서. 세력 문서와 별도로 한 세력에 소속된 기구를 적고, 소속 세력별 폴더(`벡실리아\`·`글루트페스테\`·`뤼메리-훈디아\`·`보레아 영구기관\`)로 나눈다.
- `설정\인물\` — 인물 문서. 소속 세력별 폴더(`벡실리아\`·`글루트페스테\`·`보레아 영구기관\`·`뤼메리-훈디아\`·`유랑방주\`·`시발룬\`·`오르다\`·`일행\`·`포르도레 도적단\`)로 나눈다. 일행의 반려 해파리 보글이처럼 사람이 아닌 등장 개체도 이 폴더에 둔다.
- `설정\문헌\` — 작중 문헌 문서(저서·조약·법령·문학).
- `설정\기물\` — 구문으로 만들어 낸 산출물과 병기 문서(심장 팔찌·베히터·솔라티스 등).